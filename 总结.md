# 易宿酒店预订平台 MVP 项目总结

> 撰写日期：2026-05-28
> 项目状态：**MVP 开发完成**

---

## 一、项目概述

易宿酒店预订平台 MVP 是一个完整的 B2C 酒店预订系统，包含三条核心业务链路：

1. **商户端** — 商户注册登录后录入酒店和房型，提交审核
2. **管理端** — 管理员审核、驳回、发布、下线酒店
3. **用户端** — 用户查询已发布酒店、查看详情、创建基础预订记录

### 技术栈

| 层级 | 技术选型 |
|------|----------|
| 前端（用户端） | React 18 + TypeScript + Axios + Tailwind CSS（`apps/mobile`） |
| 前端（管理端） | React 18 + TypeScript + Axios + Zustand + Tailwind CSS（`apps/web`） |
| 后端 | Nest.js + TypeScript + Prisma ORM |
| 数据库 | PostgreSQL |
| 工程架构 | Monorepo + pnpm workspace |

### 项目结构

```
hotel-All/
├── apps/
│   ├── api/          # Nest.js 后端 API（端口 3000）
│   ├── mobile/      # 移动端用户预订界面（端口 5173）
│   └── web/         # PC 管理端（商户/管理员后台，端口 5174）
├── docs/
│   ├── SPEC.md       # 需求规格说明
│   ├── MILESTONES.md # 开发里程碑
│   └── API.md        # API 接口设计
└── AGENTS.md         # Agent 团队开发规范
```

---

## 二、开发方法论：Superpowers 工作流

本项目全程使用 [obra/superpowers](https://github.com/obra/superpowers) AI 编程方法论，通过Codex 和 Mavis Agent 团队协作执行。

### 核心流程

```
brainstorming（设计细化）
    ↓
writing-plans（生成实施计划）
    ↓
subagent-driven-development（子代理驱动开发）
    ↓
requesting-code-review（代码审查）
    ↓
finishing-a-development-branch（分支完成决策）
```

### 关键原则

- **TDD（测试驱动）** — 先写失败测试，再写最小实现代码，最后重构
- **YAGNI** — 不提前实现不需要的功能
- **DRY** — 拒绝重复代码
- **系统性验证** — 证据先行，完成前验证
- **每次只实现一个 Milestone** — 避免范围蔓延
- **分支开发** — 每个 Milestone 新建独立分支，完成后 PR 合并

### 使用的 Skill

| Skill | 用途 |
|-------|------|
| `brainstorming` | 苏格拉底式设计细化 |
| `writing-plans` | 详细实施计划生成 |
| `subagent-driven-development` | 子代理驱动开发（两阶段审查） |
| `executing-plans` | 批量执行与检查点 |
| `test-driven-development` | RED-GREEN-REFACTOR 测试循环 |
| `systematic-debugging` | 4阶段根因分析调试 |
| `verification-before-completion` | 完成前验证 |
| `using-git-worktrees` | Git worktree 并行开发 |
| `requesting-code-review` | 代码审查流程 |
| `receiving-code-review` | 响应审查反馈 |
| `finishing-a-development-branch` | 分支完成决策流 |

---

## 三、Milestone 开发记录

### Milestone 1：项目基础结构
**目标：** 搭建前后端和数据库开发基础

**完成内容：**
- 初始化 Monorepo 和 pnpm workspace
- 创建 `apps/api`、`apps/mobile`、`apps/web` 三个应用
- 配置 TypeScript、Tailwind CSS
- 配置 Prisma 和 PostgreSQL 连接
- 配置基础启动脚本

**分支：** `codex/milestone-1-init`

---

### Milestone 2：核心数据模型
**目标：** 建立 MVP 所需的数据结构

**完成内容：**
- 创建用户模型（USER、MERCHANT、ADMIN 三种角色）
- 创建酒店模型（含状态：待审核、审核不通过、审核通过、已发布、已下线）
- 创建房型模型
- 创建预订模型
- 创建审核记录和驳回原因字段
- 添加 seed 数据（管理员账号和测试数据）

**分支：** `codex/milestone-2-models`

---

### Milestone 3：登录注册和角色权限
**目标：** 完成基础账号体系和角色访问控制

**完成内容：**
- 实现商户注册和登录
- 实现普通用户注册和登录
- 实现管理员登录
- 登录后返回 JWT token
- 根据 USER、MERCHANT、ADMIN 控制接口访问权限
- 角色守卫（RolesGuard）和权限校验
- ADMIN 账号通过系统预置，不开放普通注册

**分支：** `codex/milestone-3-auth`

---

### Milestone 4：商户酒店管理
**目标：** 让商户可以录入和维护自己的酒店与房型

**完成内容：**
- 商户创建酒店（含所有必填字段校验）
- 商户编辑自己创建的酒店
- 商户维护自己酒店的房型和价格
- 保存后酒店进入「待审核」状态
- 商户查看自己酒店的审核状态和驳回原因
- 房型变更后自动重置酒店状态为待审核

**分支：** `codex/milestone-4-merchant-hotels`

---

### Milestone 5：管理员审核发布
**目标：** 让管理员控制酒店是否可以在用户端展示

**完成内容：**
- 管理员查看待审核酒店列表
- 审核通过（待审核 → 审核通过）
- 审核驳回（填写驳回原因）
- 发布酒店（审核通过 → 已发布）
- 下线酒店（已发布 → 已下线）
- 恢复发布（已下线 → 已发布）
- 状态流转规则集中校验在审核服务层

**分支：** `codex/milestone-5-admin-audit-publish`

---

### Milestone 6：用户端酒店查询和详情
**目标：** 让用户可以找到并查看已发布酒店

**完成内容：**
- 酒店查询页（城市/地点、关键字、入住/离店日期）
- 酒店列表页（只展示已发布酒店）
- 列表展示：名称、地址、星级、最低价格、图片
- 酒店详情页（展示酒店信息、设施、房型）
- 房型按价格从低到高排序
- 用户端所有接口强制过滤「已发布」状态

**分支：** `codex/milestone-6-user-hotel-search-detail`

---

### Milestone 7：基础预订记录
**目标：** 让登录用户可以基于酒店房型创建预订记录

**完成内容：**
- 普通用户注册、登录、token 存储
- 移动端用户认证入口
- 预订面板（房型选择、入住人数、日期确认）
- 总价由后端根据房型价格和间夜数计算
- 角色边界：只有 USER 可创建预订
- 酒店状态校验：只允许预订已发布酒店

**分支：** `codex/milestone-7-basic-booking`

---

### Milestone 8：MVP 闭环验收
**目标：** 验证 MVP 主线完整可用

**完成内容：**
- 后端 7 条主链路回归验证
- 移动端用户预订闭环验收
- PC 端角色边界验证
- 质量门禁：`pnpm lint` / `pnpm typecheck` / `pnpm test` / `pnpm build`
- 反向验收：越权角色拦截、下线酒店不可见

**分支：** `codex/milestone-8-mvp-acceptance`

---

## 四、核心模块一览

### 后端模块（`apps/api/src/`）

| 模块 | 路径 | 职责 |
|------|------|------|
| `auth` | `src/auth/` | 注册、登录、JWT、角色守卫 |
| `hotels` | `src/hotels/` | 商户酒店 CRUD |
| `rooms` | `src/rooms/` | 商户房型管理 |
| `audit` | `src/audit/` | 管理员审核、发布、下线 |
| `bookings` | `src/bookings/` | 用户预订记录 |
| `prisma` | `src/prisma/` | Prisma 服务和模块 |
| `common` | `src/common/` | 全局异常过滤器 |

### 前端模块

**管理端（`apps/web/`）**
- `AuthForm.tsx` — 登录/注册表单
- `MerchantHotelManager.tsx` — 商户酒店与房型管理
- `AdminHotelAuditManager.tsx` — 管理员审核工作区

**用户端（`apps/mobile/`）**
- `MobileHotelSearch.tsx` — 酒店查询
- `MobileHotelList.tsx` — 酒店列表
- `MobileHotelDetail.tsx` — 酒店详情
- `MobileBookingPanel.tsx` — 预订面板
- `MobileUserAuth.tsx` — 用户登录注册

---

## 五、API 端点一览

### 认证（`/auth`）
- `POST /auth/register` — 注册（USER/MERCHANT）
- `POST /auth/login` — 登录
- `GET /auth/me` — 获取当前用户

### 酒店（`/hotels`）
- `GET /hotels` — 用户端酒店列表（公开，已发布）
- `GET /hotels/:id` — 用户端酒店详情（公开，已发布）
- `GET /hotels/my` — 商户酒店列表
- `POST /hotels` — 商户创建酒店
- `PATCH /hotels/:id` — 商户编辑酒店

### 房型（`/rooms`）
- `GET /hotels/:hotelId/rooms` — 公开房型查询
- `POST /rooms` — 商户创建房型
- `PATCH /rooms/:id` — 商户编辑房型

### 审核（`/audit`）
- `GET /audit/hotels` — 审核酒店列表
- `PATCH /audit/hotels/:id/approve` — 审核通过
- `PATCH /audit/hotels/:id/reject` — 审核驳回
- `PATCH /audit/hotels/:id/publish` — 发布酒店
- `PATCH /audit/hotels/:id/offline` — 下线酒店

### 预订（`/bookings`）
- `POST /bookings` — 用户创建预订记录

---

## 六、Git 提交记录

| 提交 | 说明 |
|------|------|
| `f892213` | fix(bug): 解决路由的路径问题 |
| `eaf0fbb` | fix(Bug): 清理页面中开发阶段遗留的说明型、演示型、调试型 UI |
| `a64a05f` | refactor: 项目 src 结构的统一化 |
| `c7d28d1` | docs: update milestone superpowers workflow skill and AGENTS.md |
| `afd74d0` | docs: 添加 ReadMe.md |
| `4f30b34` | test: 项目开发完结，add milestone 8 acceptance coverage |
| `8457a72` | feat: complete milestone 7 basic booking |
| `b563ca5` | feat: complete milestone 6 user hotel experience |
| `1d358cd` | feat: add milestone 5 admin audit workflow |
| `b364696` | feat: 完成 Milestone 4 商户酒店管理 |
| `ec2e03a` | chore: add milestone 3 auth |
| `5f1a44a` | chore: add milestone 2 core data models |
| `f33fda5` | chore: initialize milestone 1 project structure |
| `88d2955` | docs: initialize hotel MVP documentation |

---

## 七、验证命令

项目根目录下执行：

```bash
pnpm lint          # ESLint 代码风格检查
pnpm typecheck     # TypeScript 类型检查
pnpm test          # 运行所有测试
pnpm build         # 生产构建
```

---

## 八、MVP 验收标准达成情况

| 验收项 | 状态 |
|--------|------|
| 商户可以创建酒店和房型，并提交审核 | ✅ |
| 管理员可以审核通过并发布酒店 | ✅ |
| 已发布酒店可以出现在用户端列表中 | ✅ |
| 用户可以查看酒店详情和房型价格 | ✅ |
| 登录用户可以选择房型并创建基础预订记录 | ✅ |
| 下线酒店不会出现在用户端 | ✅ |
| 不同角色只能访问自己有权限的功能 | ✅ |

---



## 九、经验与记录

### Superpowers 工作流收益
- **计划先行** — 每个 Milestone 先写计划文档，明确文件路径和任务边界，避免实现时迷失
- **TDD 驱动** — 先写失败测试再实现，代码质量可控
- **分支隔离** — 每个 Milestone 独立分支，PR 合并保证主线干净
- **两阶段审查** — 子代理实现后先规范合规性审查，再代码质量审查

### 项目规范沉淀
- 前端 UI 开发必须使用项目内 `.agents/skills/ele-ui` Skill，保持风格统一
- 所有 API 接口统一响应格式 `{ code, message, data }`
- 状态流转规则集中在校验服务层，避免规则不一致
- 用户端强制过滤非已发布酒店，后端是最后一道防线

---

